//! Monkeybee PDF forensics: hidden-content, redaction-audit, and signature-change analysis.

/// Placeholder forensics surface until the analysis pipeline is implemented.
#[derive(Debug, Default, Clone, Copy, PartialEq, Eq)]
pub struct ForensicsStatus;

impl ForensicsStatus {
    /// Returns the placeholder status for the current crate revision.
    pub fn placeholder() -> Self {
        Self
    }
}
